import { StyleSheet } from 'react-native'

const size = {
    // global sizes
    base: 8,
    font: 16,
    radius: 30,
    padding: 26,
}

export default StyleSheet.create({
    Base: {
        width: size.base,
        height: size.base
    },
})